# Server environment configuration template
# AI Studio injects secret values at runtime via platform settings.
# NEVER commit real secret values, keys, or tokens to this template.

GEMINI_API_KEY=
APP_URL=
AUTHORIZED_DOMAIN="https://work.forenclue.in"

# Backend Cloud Storage Configuration (Server-side only)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_MAX_FILE_SIZE_MB="25"

# Storage endpoints (optional)
R2_PUBLIC_URL=
STORAGE_PUBLIC_BASE_URL=
VITE_STORAGE_API_URL=
